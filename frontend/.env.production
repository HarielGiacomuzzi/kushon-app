# Production Environment Configuration
# This file is used for production builds

# API URL - In production on Heroku, the frontend and backend are served from the same domain
# So we can use relative paths or the same origin
VITE_API_URL=/api
